// Re-export to preserve the existing import path. The implementation now lives
// in the messaging feature module (split from the original monolith).
export { InboxScreen } from "@/features/messaging/components/InboxScreen";
